import { API_URL } from "@/app/config";

export const ENDPOINTS = {
    CATEGORIES: `${API_URL}/categories`,
    ARTICLES: `${API_URL}/articles`,
    AUTHORS: `${API_URL}/authors`,
    MAGAZINES: `${API_URL}/magazines`,
    CONTACT_INFO: `${API_URL}/contact`,
    MANAGER_EMAIL: `${API_URL}/manager-email`,
}

